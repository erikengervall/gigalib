{
  "name": "mrgen",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "----- CI": "",
    "ci:build": "tsc",
    "ci:format": "prettier --check './src/**/*.{ts,tsx}' --ignore-path ../../.prettierignore",
    "ci:lint": "eslint 'src/**/*.{ts,tsx}'",
    "ci:test:unit": "jest",
    "----- LOCAL DEV": "",
    "dev": "rm -rf dist && tsc --watch",
    "----- MISC": "",
    "format:write": "prettier --write './src/**/*.{ts,tsx}'",
    "clean:reset": "rm -rf node_modules tsconfig.tsbuildinfo yarn-error.log dist"
  },
  "dependencies": {
    "qs": "^6.11.2",
    "zod": "^3.21.4",
    "zod-validation-error": "^1.3.1",
    "shared": "*"
  }
}
